services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: talentos-api
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - .:/app
    environment:
      - WATCHFILES_FORCE_POLLING=true
    depends_on:
      - chromadb

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: talentos-frontend
    ports:
      - "3000:3000"
    env_file: ./frontend/.env.local
    depends_on:
      - api
      - chromadb

  chromadb:
    image: chromadb/chroma:latest
    container_name: talentos-chroma
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/chroma/data
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE

  # Optional: Local Postgres for development if not using Supabase
  # postgres:
  #   image: postgres:15
  #   container_name: talentos-postgres
  #   environment:
  #     POSTGRES_USER: talent
  #     POSTGRES_PASSWORD: talent
  #     POSTGRES_DB: talent_intelligence
  #   ports:
  #     - "5432:5432"

  # redis:
  #   image: redis:7
  #   container_name: talentos-redis
  #   ports:
  #     - "6379:6379"

volumes:
  chroma-data:
